# Provider selection — "mock" requires no API key and works out of the box.
# Switch to a live provider once you have the matching key below.
STT_PROVIDER=mock
LLM_PROVIDER=mock
TTS_PROVIDER=mock

# Live provider keys (only needed if the matching *_PROVIDER above is not "mock")
OPENAI_API_KEY=
DEEPGRAM_API_KEY=
ELEVENLABS_API_KEY=

# Server
PORT=8787
