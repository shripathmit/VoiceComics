{
  "name": "voicecomics",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "client",
    "server",
    "packages/types"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c green,cyan \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w packages/types && npm run build -w server && npm run build -w client",
    "typecheck": "npm run typecheck -w packages/types && npm run typecheck -w server && npm run typecheck -w client",
    "test": "npm run test -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.6.3"
  }
}
